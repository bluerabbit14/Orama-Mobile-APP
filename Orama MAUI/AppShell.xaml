<?xml version="1.0" encoding="UTF-8" ?>
<Shell  
    x:Class="Orama.AppShell"  
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"  
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"  
    xmlns:local="clr-namespace:Orama"  
    xmlns:pages="clr-namespace:Orama.Pages" 
    xmlns:vm="clr-namespace:Orama.Models.ViewModels" 
    Shell.FlyoutBehavior="Flyout"  
    Shell.TabBarIsVisible="True"
    Shell.TabBarBackgroundColor="{AppThemeBinding Light=#fff,Dark=#fff}"
    Shell.TabBarUnselectedColor="{AppThemeBinding Light=#C8C8C8,Dark=#C8C8C8}"
    Title="Orama">

    <Shell.FlyoutContentTemplate >
        <DataTemplate>
            <Grid RowDefinitions="Auto,*,Auto" Padding="10" Margin="10">
                <Grid.BindingContext>
                    <vm:flyoutContentViewModel />
                </Grid.BindingContext>
                <!-- Header -->
                <StackLayout Grid.Row="0" Margin="10,5">
                    <Grid ColumnDefinitions="auto,*,auto" Padding="0,0,0,10">
                        <Border Grid.Column="0"  HorizontalOptions="Start">
                            <Border.Stroke>
                                <SolidColorBrush Color="Transparent"/>
                            </Border.Stroke>
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="35"/>
                            </Border.StrokeShape>
                            <Image Source="Image.png" WidthRequest="45" HeightRequest="45">
                                <Image.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="Image_Tapped"/>
                                </Image.GestureRecognizers>
                            </Image>
                        </Border>
                        <Image Grid.Column="2" Source="about.png" HeightRequest="25" VerticalOptions="Center">
                            <Image.GestureRecognizers>
                                <TapGestureRecognizer Tapped="About_Tapped"/>
                            </Image.GestureRecognizers>
                        </Image>
                    </Grid>
                    <Label x:Name="Username"  Text="@being.sif" FontAttributes="Bold"/>
                    <Label x:Name="Email"  Text="14asifcr7@gmail.com"/>
                    <StackLayout Orientation="Horizontal" Spacing="10" Margin="0,5,0,0">
                        <HorizontalStackLayout Spacing="5">
                            <Label Text="0" FontAttributes="Bold"/>
                            <Label Text="Followers"/>
                        </HorizontalStackLayout>
                        <HorizontalStackLayout Spacing="5">
                            <Label Text="0" FontAttributes="Bold"/>
                            <Label Text="Following"/>
                        </HorizontalStackLayout>
                    </StackLayout>
                    <BoxView HeightRequest=".1" Margin="2"/>
                </StackLayout>

                <!--Main Content -->
                    <CollectionView Grid.Row="1" ItemsSource="{Binding FlyoutContents}"
                        SelectionMode="Single"
                        SelectedItem="{Binding FlyoutContent}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <StackLayout Orientation="Horizontal" Padding="10">
                                    <Image Source="{Binding Icon}" WidthRequest="30" HeightRequest="30" />
                                    <Label Text="{Binding Title}" Margin="10,0,0,0" FontSize="20" />
                                </StackLayout>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                    <!-- Footer -->
                <StackLayout Grid.Row="2" VerticalOptions="End" Background="Transparent">
                    <Image Source="{AppThemeBinding Light=sun.png, Dark=moon.png}" HeightRequest="30" HorizontalOptions="Start"/>
                </StackLayout>
            </Grid>
        </DataTemplate>
    </Shell.FlyoutContentTemplate>

    <TabBar>  
        <ShellContent Title="Home" Icon="{AppThemeBinding Light=home.png,Dark=home.png}" ContentTemplate="{DataTemplate pages:Home}"  Route="Home" />
        <ShellContent Title="Search" Icon="{AppThemeBinding Light=search.png,Dark=search.png}" ContentTemplate="{DataTemplate pages:Search}"  Route="Search" />
        <ShellContent Title="Profile" Icon="{AppThemeBinding Light=profile.png,Dark=profile.png}" ContentTemplate="{DataTemplate pages:Profile}"  Route="Profile" />
    </TabBar>  

</Shell>
